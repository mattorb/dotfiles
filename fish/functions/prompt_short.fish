function prompt_short --description 'Short prompt.  Drops right prompt/etc for easier copy/paste, screen recording, etc, but keeps the directory on the left'
	source ~/.config/fish/functions/ancillary/prompt_short.fish
end
